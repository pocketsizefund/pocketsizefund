[project]
name = "positionmanager"
version = "20240903.2"
description = "Service to handle trading based off of upstream model predictions"
requires-python = ">=3.9, <3.11"
dependencies = [
    "fastapi>=0.112.2",
    "loguru",
    "pocketsizefund[trade]",
    "pydantic>=2.8.2",
    "uvicorn>=0.30.6",
]
