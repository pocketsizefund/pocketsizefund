# data = pl.read_csv("consolidated_data.csv")

# preprocessor = Preprocessor(data)

# preprocessed_data = preprocessor.normalize(data)

# dataloader = DataLoader(preprocessed_data)

# ==============================================================================

# outline:
# [x] preprocessor
# [x] data loader
