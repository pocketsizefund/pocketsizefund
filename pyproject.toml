[project]
name = "pocketsizefund"
version = "0.1.0"
description = "open source quantitative hedge fund 🍊"
readme = "README.md"
requires-python = ">=3.12"
dependencies = []

[tool.uv.workspace]
members = ["infrastructure", "workflows"]

[dependency-groups]
dev = [
    "pytest>=8.3.5",
]
