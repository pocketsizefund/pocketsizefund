#!/usr/bin/env bash

# compiles SAM template resources

set -e

environment="development"
if [[ "$1" == "production" ]]; then
    environment="production"
fi

sam build --use-container --cached --parallel --config-env $environment
