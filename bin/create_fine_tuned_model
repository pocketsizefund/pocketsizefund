#!/usr/bin/env bash

# create fine-tuned model in
# OpenAI and return the model ID

set -e

fine_tune_model_id=$(python3 ./bin/helpers/created_fine_tuned_model.py) 

echo '{"fine_tune_model_id": "$fine_tune_model_id"}' >> fine_tune_model_id.json

echo fine tune model id: $fine_tune_model_id
