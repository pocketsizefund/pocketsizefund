#!/bin/bash

# download model artifacts
# from S3

set -e

model_name=$1

if [[ -z "${model_name}" ]]; then
    echo "model name is required"
    exit 1
fi

aws s3 cp s3://pocketsizefund-artifacts/models/${model_name} .

tar -xzf model.tar.gz

rm -r testing_data/
