#!/usr/bin/env bash

# invoke inference endpoint
# in SageMaker

set -e

if [[ "$1" == "remote" ]]; then
    python3 ./bin/helpers/invoke_endpoint.py
elif [[ "$1" == "local" ]]; then
    python3 ./bin/helpers/invoke_local_endpoint.py
fi
