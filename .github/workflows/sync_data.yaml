---
name: Sync data
on:
  schedule:
    - cron: 0 10 * * 1,2,3,4,5 # launch at 5:00 AM EST
jobs:
  fetch_data:
    name: Sync data on weekday schedule
    runs-on: ubuntu-latest
    environment: pulumi
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      - name: Install Flox
        uses: flox/install-flox-action@v2
      - name: Fetch with Pulumi
        uses: flox/activate-action@v1
        env:
          PULUMI_ACCESS_TOKEN: ${{ secrets.PULUMI_ACCESS_TOKEN }}
        with:
          command: mask infrastructure services invoke datamanager
